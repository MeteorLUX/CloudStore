# 路径与秒传安全要点

- 所有用户路径都经 PathGuard，不能靠客户端传来的绝对路径访问磁盘。
- 对象库 `objects/` 不在用户逻辑命名空间内，客户端无法 ls / 删除别人的对象。
- 秒传先做切片持有证明，避免只提交 MD5 就拿走对象库里的文件。
- 秒传命中后通过引用计数增加 `user_id + virtual_path -> md5` 映射，不拷贝物理文件；下载始终从只读对象库读取。
- 删除文件时 `removeRef`；仅当全局引用数为 0 时才 GC 删除 `objects/` 中的内容，其他用户的引用不受影响。
